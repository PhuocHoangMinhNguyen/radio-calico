# pnpm configuration for Radio Calico

# Use hoisted node_modules (similar to npm) for better compatibility with Angular
node-linker=hoisted

# Automatically install peer dependencies
auto-install-peers=true

# Strict peer dependencies (warn instead of fail)
strict-peer-dependencies=false

# Use shamefully-hoist for maximum compatibility
shamefully-hoist=true

# Public hoist pattern for Angular and common packages
public-hoist-pattern[]=@angular/*
public-hoist-pattern[]=@playwright/*
public-hoist-pattern[]=vitest

# Store directory (defaults to ~/.pnpm-store)
# Uncomment to use project-local store
# store-dir=.pnpm-store

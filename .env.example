# .env.example — Environment Variable Template for Radio Calico
# Copy this file to .env and fill in actual values
# NEVER commit .env to version control

# ============================================================================
# PostgreSQL Database Configuration
# ============================================================================

# Database host (default: localhost for local dev, 'db' for Docker)
PGHOST=localhost

# Database port (default: 5432)
PGPORT=5432

# Database name
PGDATABASE=radio_calico

# Database user
PGUSER=postgres

# Database password [SECRET - REQUIRED]
# Generate a strong password for production:
#   openssl rand -base64 32
PGPASSWORD=your_secure_password_here

# ============================================================================
# Application Configuration
# ============================================================================

# Server port (default: 3000 for production, 3001 for dev API)
PORT=3000

# Node environment (development, production, test)
NODE_ENV=production

# Application port for docker-compose.prod.yml host binding (optional)
APP_PORT=3000

# ============================================================================
# Development vs Production Notes
# ============================================================================
#
# Development (docker-compose.yml):
#   - Uses default password: radiocalico_dev_password
#   - Exposes PostgreSQL on host port 5432
#   - Includes Adminer UI on port 8080
#   - Hot-reload enabled with volume mounts
#
# Production (docker-compose.prod.yml):
#   - REQUIRES setting PGPASSWORD in .env
#   - PostgreSQL NOT exposed to host (internal network only)
#   - No Adminer included
#   - Read-only containers with security hardening
#
# ============================================================================
# Security Checklist for Production
# ============================================================================
#
# [ ] Set a strong, unique PGPASSWORD (minimum 32 characters)
# [ ] Never commit .env to version control
# [ ] Use a secrets management service (AWS Secrets Manager, etc.)
# [ ] Rotate passwords regularly
# [ ] Enable SSL/TLS for database connections in cloud deployments
# [ ] Review and restrict container network access
# [ ] Use environment-specific .env files (.env.production, .env.staging)
#
